const Router = require('express')
const router = new Router()
 
router.get('/list',)
router.get('/sort/category/:name',)
router.get('/sort/janre/:name',)
router.get('/sort/mark/:name',)

module.exports = router